export class Contract {
    constructor() {
        //部署合约
        this.deploy = () => {
            return "hi";
        };
        //执行有参合约
        this.execute = (args) => {
            return "hi";
        };
        //执行无参合约
        this.call = () => {
            return "hi";
        };
    }
}
